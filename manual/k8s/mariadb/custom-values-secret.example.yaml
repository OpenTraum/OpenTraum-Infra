# Copy this file to custom-values-secret.yaml and fill in real values.
# custom-values-secret.yaml is gitignored. Apply at install time:
#   helm upgrade --install mariadb bitnami/mariadb \
#     -f custom-values.yaml -f custom-values-secret.yaml
auth:
  rootPassword: "REPLACE_ME"
  password: "REPLACE_ME"
