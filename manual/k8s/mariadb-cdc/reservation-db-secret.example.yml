---
# Copy this file to reservation-db-secret.yml and fill in real values.
# reservation-db-secret.yml is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: reservation-db-secret
  namespace: kafka
  labels:
    app: reservation-db
    app.kubernetes.io/part-of: opentraum
    app.kubernetes.io/component: database
type: Opaque
stringData:
  mariadb-root-password: "REPLACE_ME"
  mariadb-password: "REPLACE_ME"
  mariadb-user: "opentraum"
  mariadb-database: "reservation"
