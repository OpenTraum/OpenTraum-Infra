---
# Copy this file to event-db-secret.yml and fill in real values.
# event-db-secret.yml is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: event-db-secret
  namespace: kafka
  labels:
    app: event-db
    app.kubernetes.io/part-of: opentraum
    app.kubernetes.io/component: database
type: Opaque
stringData:
  mariadb-root-password: "REPLACE_ME"
  mariadb-password: "REPLACE_ME"
  mariadb-user: "opentraum"
  mariadb-database: "event"
