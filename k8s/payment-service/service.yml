apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: opentraum
  labels:
    app: payment-service
    app.kubernetes.io/part-of: opentraum
    app.kubernetes.io/component: payment
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP
